apiVersion: v1
kind: Service
metadata:
  name: flask-api-service
  namespace: default
  labels:
    app: flask-api
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 5000
    protocol: TCP
    name: http
  selector:
    app: flask-api
